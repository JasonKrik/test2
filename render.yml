services:
  - name: web
    type: web
    runtime: image
    image:
      url: docker.io/library/nginx:alpine
    startCommand: ""
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: "80"
